# Voice Capabilities

chatVSP voice flow supports:

- STT (microphone transcription)
- TTS (spoken responses)
- WebSocket streaming for transcription paths

Requirements:

- voice providers configured in admin settings
- `WEB_DOMAIN` matches public origin
- proxy supports WebSocket upgrades
